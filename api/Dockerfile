# SolarIQ FastAPI serving layer.
#
# Built with the repository root as context (matching orchestration/Dockerfile),
# so the image only ever contains api/, not the sibling subsystems.

FROM python:3.12-slim
WORKDIR /app

COPY api/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY api/app ./app

ENV PYTHONUNBUFFERED=1
EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
